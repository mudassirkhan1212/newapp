<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <!-- Header -->
    <div class="app-header">
      <h1><i class="bi bi-lightning-charge-fill text-primary me-1"></i>App</h1>
      <div class="d-flex gap-2">
        <button
          class="btn btn-sm btn-outline-secondary rounded-3"
          data-bs-toggle="modal"
          data-bs-target="#authModal"
        >
          <i class="bi bi-person me-1"></i>Sign In
        </button>
      </div>
    </div>

    <!-- ══════════ POST MODAL ══════════ -->
    <div
      class="modal fade"
      id="postModal"
      tabindex="-1"
      data-bs-backdrop="static"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg">
          <div class="modal-header border-0 px-4 pt-4 pb-2">
            <div class="d-flex align-items-center gap-2">
              <div
                class="avatar rounded-circle bg-primary bg-gradient d-flex align-items-center justify-content-center text-white fw-bold"
                style="width: 38px; height: 38px; font-size: 0.9rem"
              >
                J
              </div>
              <div>
                <div
                  class="fw-semibold"
                  style="font-size: 0.9rem; line-height: 1.2"
                >
                  John Doe
                </div>
                <div class="text-muted" style="font-size: 0.72rem">
                  <i class="bi bi-globe2 me-1"></i>Public
                </div>
              </div>
            </div>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
            ></button>
          </div>

          <div class="modal-body px-4 py-2">
            <form id="postForm">
              <textarea
                class="form-control border-0 bg-transparent p-0 mb-3"
                name="caption"
                id="captionInput"
                rows="3"
                maxlength="300"
                placeholder="What's on your mind, John?"
                style="
                  resize: none;
                  font-size: 1rem;
                  outline: none;
                  box-shadow: none;
                "
              ></textarea>

              <div id="previewWrapper" class="position-relative mb-3 d-none">
                <img
                  id="imagePreview"
                  alt="Preview"
                  class="w-100 rounded-3"
                  style="max-height: 260px; object-fit: cover"
                />
                <button
                  type="button"
                  id="removeBtn"
                  class="btn btn-dark btn-sm position-absolute top-0 end-0 m-2 rounded-circle d-flex align-items-center justify-content-center p-0"
                  style="width: 28px; height: 28px; opacity: 0.8"
                >
                  <i class="bi bi-x"></i>
                </button>
              </div>

              <div class="upload-zone mb-3" id="uploadZone">
                <input
                  type="file"
                  name="image"
                  id="imageInput"
                  accept="image/*"
                  style="
                    position: absolute;
                    inset: 0;
                    opacity: 0;
                    cursor: pointer;
                  "
                />
                <i class="bi bi-image fs-2 mb-1 text-primary"></i>
                <span style="font-size: 0.82rem"
                  >Click or drag to add a photo</span
                >
              </div>

              <div
                class="text-end text-muted mb-2"
                id="charCount"
                style="font-size: 0.75rem"
              >
                0 / 300
              </div>

              <div
                class="d-flex align-items-center justify-content-between border-top pt-3 pb-1"
              >
                <div class="d-flex gap-2">
                  <button
                    type="button"
                    class="btn btn-light btn-sm rounded-3 text-muted"
                  >
                    <i class="bi bi-emoji-smile me-1"></i>Emoji
                  </button>
                  <button
                    type="button"
                    class="btn btn-light btn-sm rounded-3 text-muted"
                  >
                    <i class="bi bi-geo-alt me-1"></i>Location
                  </button>
                </div>
                <button
                  type="submit"
                  id="postBtn"
                  class="btn btn-primary rounded-3 fw-semibold px-4"
                  style="
                    background: linear-gradient(135deg, #0d6efd, #6610f2);
                    border: none;
                  "
                  disabled
                >
                  <i class="bi bi-send me-1"></i>Post
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- ══════════ AUTH MODAL ══════════ -->
    <div
      class="modal fade"
      id="authModal"
      data-bs-backdrop="static"
      data-bs-keyboard="false"
      tabindex="-1"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg">
          <div class="modal-header border-0 pb-0 px-4 pt-3 justify-content-end">
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
            ></button>
          </div>

          <div class="modal-body px-4 pb-4 auth-panels">
            <!-- Tab Bar -->
            <!-- <div class="tab-bar d-flex mb-0">
              <button
                class="auth-tab-btn active"
                id="tabSignIn"
                onclick="switchTab('signin')"
              >
                Sign In
              </button>
              <button
                class="auth-tab-btn"
                id="tabSignUp"
                onclick="switchTab('signup')"
              >
                Sign Up
              </button>
            </div> -->

            <!-- Sign In -->
            <div class="panel" id="panelSignIn">
              <div class="text-center mb-4">
                <!-- <h5 class="fw-bold mb-1">Welcome Back 👋</h5> -->
                <p class="text-muted small mb-0">
                  Sign in to access your account
                </p>
              </div>
              <div class="form-floating mb-3">
                <input
                  type="email"
                  class="form-control rounded-3"
                  id="siEmail"
                  placeholder="name@example.com"
                />
                <label for="siEmail"
                  ><i class="bi bi-envelope me-2"></i>Email address</label
                >
              </div>
              <div class="form-floating mb-3">
                <input
                  type="password"
                  class="form-control rounded-3"
                  id="siPassword"
                  placeholder="Password"
                />
                <label for="siPassword"
                  ><i class="bi bi-lock me-2"></i>Password</label
                >
              </div>
              <div class="d-flex justify-content-between mb-3 small">
                <label
                  class="d-flex align-items-center gap-1"
                  style="cursor: pointer"
                >
                  <input class="form-check-input mt-0" type="checkbox" />
                  Remember me
                </label>
                <a href="#" class="text-decoration-none">Forgot password?</a>
              </div>
              <button class="btn btn-primary w-100 rounded-3 py-2 fw-semibold">
                Sign In
              </button>
              <div class="divider-or">OR</div>
              <div
                id="g_id_onload"
                data-client_id="476730264996-hrlisknlp3csc2hth4lqk3j2ik18a1up.apps.googleusercontent.com"
                data-callback="handleGoogleSignIn"
                data-auto_prompt="false"
              ></div>
              <div
                class="g_id_signin"
                data-type="standard"
                data-theme="outline"
                data-text="continue_with"
                data-shape="rectangular"
                data-width="100%"
              ></div>
              <p class="text-center mt-4 small text-muted mb-0">
                Don't have an account?
                <a
                  href="#"
                  class="text-decoration-none fw-semibold"
                  onclick="
                    switchTab('signup');
                    return false;
                  "
                  >Sign up</a
                >
              </p>
            </div>

            <!-- Sign Up -->
            <div class="panel hidden" id="panelSignUp">
              <div class="text-center mb-4">
                <!-- <h5 class="fw-bold mb-1">Create an Account 🚀</h5> -->
                <p class="text-muted small mb-0">Sign up to post</p>
              </div>
              <div class="form-floating mb-3">
                <input
                  type="email"
                  class="form-control rounded-3"
                  id="suEmail"
                  placeholder="name@example.com"
                />
                <label for="suEmail"
                  ><i class="bi bi-envelope me-2"></i>Email address</label
                >
              </div>
              <div class="form-floating mb-3">
                <input
                  type="password"
                  class="form-control rounded-3"
                  id="suPassword"
                  placeholder="Password"
                />
                <label for="suPassword"
                  ><i class="bi bi-lock me-2"></i>Password</label
                >
              </div>
              <button class="btn btn-primary w-100 rounded-3 py-2 fw-semibold">
                Create Account
              </button>
              <div class="divider-or">OR</div>
              <div
                id="g_id_onload"
                data-client_id="476730264996-hrlisknlp3csc2hth4lqk3j2ik18a1up.apps.googleusercontent.com"
                data-callback="handleGoogleSignIn"
                data-auto_prompt="false"
              ></div>
              <div
                class="g_id_signin"
                data-type="standard"
                data-theme="outline"
                data-text="continue_with"
                data-shape="rectangular"
                data-width="100%"
              ></div>
              <p class="text-center mt-4 small text-muted mb-0">
                Already have an account?
                <a
                  href="#"
                  class="text-decoration-none fw-semibold"
                  onclick="
                    switchTab('signin');
                    return false;
                  "
                  >Sign in</a
                >
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Your actual layout -->
    <section>
      <div class="container">
        <div></div>
        <div class="row">
          <div class="col"></div>
          <div class="col-md-5">
            <!-- Create Post Bar (Facebook-style trigger) -->
            <div
              class="create-post-bar"
              data-bs-toggle="modal"
              data-bs-target="#postModal"
            >
              <!-- <div class="avatar">J</div> -->
              <input type="text" placeholder="What's on your mind ?" readonly />
              <button
                class="btn btn-light btn-sm rounded-3 text-muted flex-shrink-0"
              >
                <i class="bi bi-plus-lg text-success"></i>
              </button>
            </div>
            <!-- ══════════ FEED ══════════ -->
            <div id="feed">
              <p class="loading">
                <i class="bi bi-arrow-repeat"></i> Loading posts...
              </p>
            </div>
          </div>
          <div class="col"></div>
        </div>
      </div>
    </section>
    <style>
      div.polaroid {
        background-color: white;
        box-shadow:
          0 4px 8px 0 rgba(0, 0, 0, 0.2),
          0 6px 20px 0 rgba(0, 0, 0, 0.19);
        margin-bottom: 5px;
      }

      div.containerr {
        padding: 15px 15px;
      }
    </style>
    <script>
      function handleGoogleSignIn(response) {
        fetch("/api/auth/google", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ idToken: response.credential }),
        })
          .then((r) => r.json())
          .then((data) => {
            if (data.error) return alert(data.error);
            localStorage.setItem("token", data.token);
            bootstrap.Modal.getInstance(
              document.getElementById("authModal"),
            ).hide();
            onAuthSuccess(data.user);
          })
          .catch(() => alert("Google sign in failed."));
      }

      // ══════════ AUTH ══════════

      function requireAuth() {
        if (localStorage.getItem("token")) return true;
        bootstrap.Modal.getOrCreateInstance(
          document.getElementById("authModal"),
        ).show();
        return false;
      }

      // Block post modal if not logged in
      document
        .getElementById("postModal")
        .addEventListener("show.bs.modal", (e) => {
          if (!requireAuth()) e.preventDefault();
        });

      function switchTab(tab) {
        const isSignIn = tab === "signin";
        document
          .getElementById("panelSignIn")
          .classList.toggle("hidden", !isSignIn);
        document
          .getElementById("panelSignUp")
          .classList.toggle("hidden", isSignIn);
        document
          .getElementById("tabSignIn")
          .classList.toggle("active", isSignIn);
        document
          .getElementById("tabSignUp")
          .classList.toggle("active", !isSignIn);
      }

      // Sign In
      document
        .querySelector("#panelSignIn .btn-primary")
        .addEventListener("click", async () => {
          const email = document.getElementById("siEmail").value.trim();
          const password = document.getElementById("siPassword").value;
          if (!email || !password) return alert("Please fill in all fields.");

          try {
            const res = await fetch("/api/auth/signin", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ email, password }),
            });
            const data = await res.json();
            if (!res.ok) return alert(data.error);
            localStorage.setItem("token", data.token);
            bootstrap.Modal.getInstance(
              document.getElementById("authModal"),
            ).hide();
            onAuthSuccess(data.user);
          } catch {
            alert("Something went wrong. Please try again.");
          }
        });

      // Sign Up
      document
        .querySelector("#panelSignUp .btn-primary")
        .addEventListener("click", async () => {
          const email = document.getElementById("suEmail").value.trim();
          const password = document.getElementById("suPassword").value;
          if (!email || !password) return alert("Please fill in all fields.");

          try {
            const res = await fetch("/api/auth/signup", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ email, password }),
            });
            const data = await res.json();
            if (!res.ok) return alert(data.error);
            localStorage.setItem("token", data.token);
            bootstrap.Modal.getInstance(
              document.getElementById("authModal"),
            ).hide();
            onAuthSuccess(data.user);
          } catch {
            alert("Something went wrong. Please try again.");
          }
        });

      function onAuthSuccess(user) {
        document.querySelectorAll(".avatar").forEach((el) => {
          el.textContent = user.email[0].toUpperCase();
        });
        const authBtn = document.querySelector('[data-bs-target="#authModal"]');
        if (authBtn) {
          authBtn.removeAttribute("data-bs-toggle");
          authBtn.removeAttribute("data-bs-target");
          authBtn.innerHTML =
            '<i class="bi bi-box-arrow-right me-1"></i>Sign Out';
          authBtn.addEventListener("click", signOut);
        }
      }

      function signOut() {
        localStorage.removeItem("token");
        location.reload();
      }

      // Restore UI on page load if already logged in
      (function init() {
        const token = localStorage.getItem("token");
        if (!token) return;
        try {
          const payload = JSON.parse(atob(token.split(".")[1]));
          if (payload.exp * 1000 < Date.now())
            return localStorage.removeItem("token");
          onAuthSuccess({ email: payload.email });
        } catch {
          localStorage.removeItem("token");
        }
      })();

      // ══════════ POST FORM ══════════

      const imageInput = document.getElementById("imageInput");
      const imagePreview = document.getElementById("imagePreview");
      const previewWrap = document.getElementById("previewWrapper");
      const uploadZone = document.getElementById("uploadZone");
      const removeBtn = document.getElementById("removeBtn");
      const captionInput = document.getElementById("captionInput");
      const charCount = document.getElementById("charCount");
      const postBtn = document.getElementById("postBtn");
      const postForm = document.getElementById("postForm");
      const feed = document.getElementById("feed");

      function checkReady() {
        postBtn.disabled = !(
          imageInput.files.length || captionInput.value.trim()
        );
      }

      function showPreview(file) {
        if (!file) return;
        imagePreview.src = URL.createObjectURL(file);
        previewWrap.classList.remove("d-none");
        uploadZone.classList.add("d-none");
        checkReady();
      }

      function clearImage() {
        imagePreview.src = "";
        previewWrap.classList.add("d-none");
        uploadZone.classList.remove("d-none");
        imageInput.value = "";
        checkReady();
      }

      imageInput.addEventListener("change", (e) =>
        showPreview(e.target.files[0]),
      );
      removeBtn.addEventListener("click", (e) => {
        e.stopPropagation();
        clearImage();
      });

      uploadZone.addEventListener("dragover", (e) => {
        e.preventDefault();
        uploadZone.style.borderColor = "#0d6efd";
        uploadZone.style.background = "#f0f4ff";
      });
      uploadZone.addEventListener("dragleave", () => {
        uploadZone.style.borderColor = "";
        uploadZone.style.background = "";
      });
      uploadZone.addEventListener("drop", (e) => {
        e.preventDefault();
        uploadZone.style.borderColor = "";
        uploadZone.style.background = "";
        const f = e.dataTransfer.files[0];
        if (f?.type.startsWith("image/")) {
          const dt = new DataTransfer();
          dt.items.add(f);
          imageInput.files = dt.files;
          showPreview(f);
        }
      });

      captionInput.addEventListener("input", () => {
        const l = captionInput.value.length;
        charCount.textContent = `${l} / 300`;
        charCount.style.color =
          l >= 300 ? "#dc3545" : l >= 250 ? "#f0ad4e" : "";
        checkReady();
      });

      // Submit Post (with auth token)
      postForm.addEventListener("submit", async (e) => {
        e.preventDefault();
        postBtn.innerHTML =
          '<span class="spinner-border spinner-border-sm me-1"></span>Posting...';
        postBtn.disabled = true;

        const formData = new FormData(postForm);
        const token = localStorage.getItem("token");

        try {
          const res = await fetch("/create-post", {
            method: "POST",
            headers: { Authorization: `Bearer ${token}` },
            body: formData,
          });
          if (!res.ok) {
            const err = await res.json();
            throw new Error(err.error);
          }

          postBtn.innerHTML = '<i class="bi bi-check-circle me-1"></i>Posted!';
          setTimeout(() => {
            bootstrap.Modal.getInstance(
              document.getElementById("postModal"),
            ).hide();
            postBtn.innerHTML = '<i class="bi bi-send me-1"></i>Post';
            clearImage();
            captionInput.value = "";
            charCount.textContent = "0 / 300";
            loadPosts();
          }, 1200);
        } catch (err) {
          alert(err.message);
          postBtn.innerHTML = '<i class="bi bi-send me-1"></i>Post';
          checkReady();
        }
      });

      // ══════════ FEED ══════════

      function renderPost(post) {
        const template = document.getElementById("post-template");
        const card = template.content.cloneNode(true);

        const body = card.querySelector(".post-body");
        const img = card.querySelector(".post-image");

        post.caption ? (body.textContent = post.caption) : body.remove();
        post.imageUrl ? (img.src = post.imageUrl) : img.remove();

        return card;
      }

      async function loadPosts() {
        feed.innerHTML =
          '<p class="loading"><i class="bi bi-arrow-repeat"></i> Loading posts...</p>';
        try {
          const res = await fetch("/posts");
          const posts = await res.json();
          feed.innerHTML = "";
          if (!posts.length) {
            feed.innerHTML =
              '<div class="empty-feed"><i class="bi bi-journal-x"></i>No posts yet. Be the first!</div>';
            return;
          }
          posts.forEach((p) => feed.appendChild(renderPost(p)));
        } catch {
          feed.innerHTML =
            '<p class="loading">Could not load posts. Make sure the server is running.</p>';
        }
      }

      loadPosts();
    </script>

    <!-- Hidden blueprint — place this anywhere, e.g. before </body> -->
    <template id="post-template">
      <div class="polaroid">
        <div class="post-body containerr"></div>
        <img class="post-image" alt="Post image" style="width: 100%" />
      </div>
    </template>
  </body>
</html>
